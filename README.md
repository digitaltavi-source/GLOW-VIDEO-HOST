# GLOW Video Host

Status: `CANDIDATE / NOT LIVE`

Public, declassified Hostinger/ChatGPT host for GLOW Video Factory.

```text
USER
  -> ChatGPT on the user's own account
  -> GLOW Video MCP Host
  -> Protected Gateway
  -> Private GLOW Video Factory
```

This repository contains only host-side transport, authentication, public contracts, deployment metadata, UI/workspace code and tests.

It must never contain KIT A/B/C internals, Capability System internals, Factory Control, private prompts, restricted evidence, private release packages or credentials.

Primary ChatGPT reasoning does not require a second LLM API call in the backend.

`PUBLIC HOST != PROTECTED FACTORY`
`MODEL_SESSION_PRIVATE != PUBLIC_DECLASSIFIED`
